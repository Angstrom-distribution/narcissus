Narcissus - Online image builder for the angstrom distribution
Koen Kooi (c) 2008 - all rights reserved 

To install this:

* put on webserver
* put opkg-cl in bin/
* put makedevs in bin/
* mkdir deploy/

To add a new machine:

* mkdir conf/<machine>
* put the angstrom-feed-configs ipkg in conf/<machine>/
* create conf/<machine>/arch.conf listing the architectures your machine supports

That's it!

